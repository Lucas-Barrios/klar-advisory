# MVP — Source Code Location

The full source code for this MVP lives in this repository's `backend/` and `frontend/` directories at the project root — see the main [repository README](../../README.md) for setup instructions.

This folder contains submission-specific documentation only:

| File | Contents |
|---|---|
| `mvp_documentation.md` | Architecture, API reference, database schema, frontend pages, local setup |
| `requirements.txt` | Python dependencies for the FastAPI backend (mirrors `backend/requirements.txt`) |
| `backend_.env.example` | Backend environment variable template |
| `frontend_.env.local.example` | Frontend environment variable template |
